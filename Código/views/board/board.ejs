<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/views/board/board.css">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title> <%= board_title %> - Notiks</title>
</head>
<body>
    <div id="base">
        <div id="options">

        </div>
        <div id="desktop">
            <% if(notes.length != 0) { %>
            <% notes.forEach((note) => { %>
                <div >
                    <h1><%= note.title %></h1>
                    <p><%= note.content %></p>
                </div>
            <% }) %>
            <% } else { %>
                <h1>Wow. Such Emptyness</h1>
            <% } %>
        </div>
    </div>
</body>
</html>